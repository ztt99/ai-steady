day10

暂存性死区

现在做提升的是判断函数内部，那么全局也需要进行变量提升


day11

Scope Lookup 作用域查找
1. 增加 binding 类
2. 增加 reference 类
3. scope 中 declare 时，保存binding 实例，将有效数据保存起来。declare只在 变量提升阶段做。
4. scope 中 resolve 时，保存 reference 实例，保存当前变量都在哪里使用，注意查询变量的时候要进行 parent 查找。
   1. shadow variable(影子变量)、闭包(closure)

day12

借助 binding、reference 检查变量的使用情况。期间测试时，缺失了一些类型，暂时补充了一部分，为了走设计流程，不需要全部完成。。。


day13
Day 13 的 Shadow Variable Detection（变量遮蔽检测）

Shadow 本质是一种 分析结果（analysis result），不是 语义结构（semantic structure）。
① 语义结构（核心模型）必须存在的基础结构：
② 分析结果（analysis result）这些是 规则产生的结果：



day14-day15

建立 graph 层结构

创建GraphEngine类，将公共方法收纳，其提供以下能力，方便依赖想调用。
edges 保存依赖关系 const a = b + c 那么 a 依赖 b 和c
reverseEdges 反向依赖 const a = b + c ,那么 b 被 a 使用
